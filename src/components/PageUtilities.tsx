'use client';

export const ADSENSE_CLIENT = 'ca-pub-3995420051808249';

/**
 * AdSense Auto Ads is loaded once globally from app/layout.tsx.
 * Individual pages may keep this component in their shell without reserving
 * an empty advertisement container; Google places Auto Ads automatically.
 */
export default function PageUtilities() {
  return null;
}
